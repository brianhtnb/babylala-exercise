import { SettingsClient } from './SettingsClient';

export const metadata = {
  title: 'Voice Settings — Babylala Exercise',
};

export default function SettingsPage() {
  return <SettingsClient />;
}
